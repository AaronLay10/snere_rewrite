{
  "name": "orchestrator-service",
  "version": "1.0.0",
  "description": "Sentient Engine - Orchestrator Service",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "clean": "rm -rf dist",
    "prisma:generate": "prisma generate --schema=../api-service/prisma/schema.prisma"
  },
  "keywords": ["sentient", "orchestrator"],
  "license": "MIT",
  "dependencies": {
    "@sentient/core-domain": "workspace:*",
    "@sentient/shared-types": "workspace:*",
    "@sentient/shared-config": "workspace:*",
    "@sentient/shared-logging": "workspace:*",
    "@sentient/shared-messaging": "workspace:*",
    "@prisma/client": "^5.22.0",
    "ioredis": "^5.3.2",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.7",
    "prisma": "^5.22.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  }
}
